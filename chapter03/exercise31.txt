—о слов автора, Ёндрю  енига - задача упражнени€ убедитьс€ в том, что читатель понимает, что дл€ алгоритма подсчета медианы нужны все значени€
https://www.daniweb.com/programming/software-development/threads/38878/accelerated-c-question-about-an-exercise

√лавное, нам нужно найти ситуации, когда будет неверное значение медианы в том случае если хот€ бы одно значение будет отброшено.

“ам же есть доказательство, поскольку сам € его не осилил

¬ любом случае, предположим, что в любой заданной точке вам известны первые M значений, отсортируйте их и выбросьте N-е (N<=M). «атем у вас есть M-N оставшихс€ чисел "справа" от отброшенного значени€ и N-1 оставшихс€ чисел слева. ≈сть три возможности:
1) N-1>M-N тогда, если неизвестный набор содержит 2n-M-1 значений больше, чем отброшенное значение, медиана-это отброшенное значение, и его невозможно найти.
2) N-1<M-N тогда, если неизвестный набор содержит M-2N+1 значений меньше, чем отброшенное значение, медиана-это отброшенное значение, и его невозможно найти.
3) N-1==M-N, то если неизвестное множество содержит равное количество больших и меньших значений, чем отброшенное значение, медиана-это отброшенное значение, и найти его невозможно.
“аким образом, всегда может быть набор скрытых значений, которые делают его таким, что отброшенное значение требуетс€ дл€ вычислени€ медианы


